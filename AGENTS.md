# AGENTS.md

## Purpose

このリポジトリは、プログラム初心者がAIと一緒に、小さなPython業務改善アプリを完成させるためのスターターキットです。

開発では、詳細な仕様を先に完成させることより、早い段階で安全な試作品を動かし、確認して分かったことを記録することを重視してください。

作業前に、次の文書を確認してください。

- [開発原則](docs/development-principles.md)：開発全体の考え方
- [推奨ディレクトリ構成](docs/project-structure.md)：新規アプリの標準的な配置
- [チャット・データ取り扱いガイド](docs/security-and-privacy.md)：共有してはいけない情報と安全な代替方法
- [コードを手作業で反映する方法](docs/copy-paste-guide.md)：チャットのコードをローカル環境へ安全に反映する案内方法

> Prototype first, document as you learn.

## Priorities

判断に迷った場合は、次の順番を優先してください。

1. 利用者のデータと既存環境を壊さない
2. 利用者が「最初の完成」に早く到達できる
3. 初心者が変更内容と確認方法を理解できる
4. 将来の自分が作業を再開できる記録を残す
5. 高度さや網羅性を追求する

## Default Course

特別な理由がなければ、開発コースは **Quick** としてください。

- **Quick**：個人用の小さな自動化。最小限をすぐ試作する
- **Standard**：継続利用または複数人利用。簡単な要求、設計、テストを残す
- **Careful**：重要データ、外部送信、認証、削除・上書きなどを扱う。実装前にリスクと復旧方法を確認する

StandardまたはCarefulが必要だと判断した場合は、理由を短く説明してください。手順を増やすこと自体を目的にしてはいけません。

## Protecting Sensitive Information

パスワード、APIキー、トークン、Cookie、個人情報、会社や顧客の機密情報などを、利用者へチャットに入力するよう求めてはいけません。

設定ファイル、ログ、スクリーンショット、表データ、業務ファイルの共有が必要になりそうな場合は、共有前に [チャット・データ取り扱いガイド](docs/security-and-privacy.md) に沿って次を案内してください。

- 本物の認証情報は `YOUR_PASSWORD`、`YOUR_API_KEY`などへ完全に置き換える
- 氏名、メールアドレス、社員番号、顧客名、ファイルパスなどを架空の値へ置き換える
- 実データではなく、同じ列構成の小さな架空データを使う
- ログや画像の背景、URL、メタデータも確認する
- 会社で利用する場合は、勤務先の情報セキュリティ規程を優先する

利用者がすでに秘密情報を入力した可能性がある場合は、その値を回答、コード、ログ、コミットへ転載しないでください。認証情報であれば、無効化または変更を案内します。

コードや設定例には架空の値だけを使用し、秘密情報は環境変数、OSの資格情報管理機能、または利用者の組織で承認された方法から読み込む設計にしてください。

## Starting a New App

新しいアプリを始めるときは、次の4項目を整理してください。

- 困りごと
- 自動化したい作業
- 入力と出力
- 最初の完成条件

利用者がすでに説明した内容を聞き直してはいけません。不足があっても、安全な仮定を置ける場合は、その仮定を明示して進めてください。

質問は、回答によって実装が大きく変わるもの、または安全に関わるものに限定してください。一度に多くの質問を並べず、原則として最も重要な質問を1つだけ尋ねます。

## Development Flow

原則として、次の順番で進めてください。

1. **相談**：4項目と開発コースを整理する
2. **試作**：代表的な入力1件を処理できる最小機能を作る
3. **確認**：実行方法と確認ポイントを示し、利用者に試してもらう
4. **改善**：実際に必要と分かった内容だけ追加する
5. **記録**：READMEや設計メモを現在の実装に合わせる
6. **完了**：完成した範囲と将来候補を分けて区切る

Quickでは、詳細な仕様書、網羅的なタスク一覧、完全な設計書を試作前の必須条件にしてはいけません。

## Project Structure

新しいアプリでは、原則として [推奨ディレクトリ構成](docs/project-structure.md) を使用してください。

- 利用者が実行する入口はルートの `main.py`、アプリ本体は `src/` に置く
- 実際の業務データ、利用者固有の設定、ログはGitHubへ登録しない
- GUI、設定、設計文書などは必要になった時点で追加する
- 新しいフォルダを作る前に、標準構成の置き場所を利用できないか確認する

既存アプリは、標準構成へ合わせることだけを目的に全面的に変更してはいけません。必要な範囲から段階的に整理し、大きな移行は利用者の同意を得てから行ってください。

## Building the First Prototype

最初の試作品では、正常系の中心機能を優先してください。

- 代表的な入力を1件扱う
- 期待する出力を1件得る
- 実行方法を簡単にする
- 利用者が結果を目で確認できるようにする

ただし、次の処理は試作段階でも事前に確認し、安全策を設けてください。

- ファイルやデータの削除
- 元ファイルへの上書き
- 大量ファイルの一括変更
- メール、チャット、Webサービスへの送信
- 認証情報や個人情報の取り扱い
- 有料APIや課金が発生する操作

可能であれば、元データを変更せず、別の出力先やテストデータを使用してください。

## Managing Scope

新しい要望や改善案は、次の3つに分類してください。

- **今回必要**：最初の完成条件を満たすために必要
- **後で改善**：使ってから判断できる
- **今は対応しない**：目的に対して効果が小さい、または複雑さが大きい

改善候補を提示する場合は、今回の完成を妨げないよう少数に絞ってください。利用者の依頼なしに、関連機能を次々に追加してはいけません。

## Communicating with Beginners

説明では、最初に結果や次の行動を伝えてください。

- 専門用語は必要なときだけ使い、短く意味を説明する
- コマンドはコピーして実行できる形で示す
- ファイル名と変更場所を具体的に示す
- エラー時は、原因候補を並べる前に最初の確認方法を示す
- 一度に大きなコードを渡さず、変更単位を小さくする
- 現在の段階と、完了までに必要なことを短く伝える

「よりよい設計」を理由に全面的な作り直しを提案する場合は、現在の方法では目的を達成できない根拠を説明してください。

## Manual Copy-and-Paste Editing

GitHub、Codex、IDE連携などでファイルを直接変更できず、利用者がチャットのコードを手作業で反映する場合は、[コードを手作業で反映する方法](docs/copy-paste-guide.md)に従ってください。

コードを提示するときは、各変更について必ず次を明記します。

1. **対象ファイル**：開くファイルの相対パス
2. **変更方法**：新規作成、全体置換、部分置換、追加のどれか
3. **変更場所**：関数名、クラス名、既存コードなどの目印
4. **確認方法**：保存後の実行手順と期待結果

さらに、次のルールを守ってください。

- 「適切な場所」「このあたり」などの曖昧な指示だけで終わらせない
- 行番号だけに頼らず、変更後も探せるコードや関数名を目印にする
- 初心者向けの貼り付けコードに、差分記号の `+` や `-`を含めない
- 「変更前」と「変更後」を別々のコードブロックで示す
- 新規ファイルと全体置換では、省略のない完全な内容を示す
- 部分変更では、置き換える範囲を過不足なく示す
- 複数ファイルの変更は、原則として1ファイルずつ案内して確認する
- 長いファイルを全体置換する前に、利用者独自の変更とバックアップの必要性を確認する
- Pythonの字下げが分かる状態でコードを示す
- 変更前のコードが一致しない場合は、推測で貼り付けず現在の該当部分を確認する

ファイルを直接変更できる環境では、手作業用の説明を毎回付ける必要はありません。ただし、変更したファイル、変更内容、確認方法は完了時に伝えてください。

## Easy Launch

最初の試作品が動作するようになったら、対象OSで利用者が簡単に起動できるファイルを作成してください。

Windowsを対象とする場合は、原則としてリポジトリ直下に `start.bat` を作成し、利用者がダブルクリックで起動できるようにします。

`start.bat`は、次の要件を満たすようにしてください。

- `cd /d "%~dp0"`などを使い、どこから実行してもアプリのフォルダを基準に動く
- アプリに合った正式な起動コマンドを使う
- 必要な場合は `requirements.txt`からライブラリを準備してから起動する
- 仮想環境は、利用者から明示的に指示された場合を除き作成・有効化しない
- PC固有の絶対パス、認証情報、利用者名を埋め込まない
- 失敗時にウィンドウがすぐ閉じず、原因と次の行動を確認できる
- 日本語を表示する場合は文字化けしないよう文字コードを考慮する

基本のPythonアプリでは `python main.py` を使います。Flet、Streamlitなど、公式に推奨される起動方法が異なる場合は、そのアプリに合ったコマンドを使用してください。

macOSまたはLinuxが対象であれば、必要に応じて `start.sh`などを用意します。対象OSが分からない場合はWindowsを既定にせず、利用者の環境を確認してください。

READMEには、コマンド操作より先に、起動用ファイルをダブルクリックする方法を記載してください。コマンドによる起動方法も、トラブル時や開発者向けの代替手段として残します。

## Python Environment and Dependencies

仮想環境は標準では使用しません。

- 利用者から明示的な指示があった場合にだけ、`venv`などの仮想環境を作成する
- 仮想環境の作成や有効化を、README、セットアップ手順、`start.bat`へ自動的に追加しない
- 必要なライブラリは原則として `requirements.txt` に記載する
- 基本のインストールコマンドは `python -m pip install -r requirements.txt` とする
- 基本の起動コマンドは `python main.py` とする

ライブラリの競合など、仮想環境が必要だと考えられる問題が発生した場合は、理由を初心者向けに説明し、利用者へ確認してください。許可なく仮想環境へ移行してはいけません。

## Python Guidelines

既存プロジェクトの方針がある場合は、そちらを優先してください。新規プロジェクトでは、必要に応じて次を採用します。

- パス操作には原則として `pathlib.Path` を使う
- 実行処理は関数へ分け、`if __name__ == "__main__":` を置く
- 外から変えたい値は、コードに埋め込まず設定ファイルや引数へ分ける
- 関数名と変数名は、役割が分かる名前にする
- 初心者が意図を判断しにくい関数には簡潔なdocstringを付ける
- エラーを握りつぶさず、利用者が次に何をすればよいか分かるメッセージを出す
- ログ、型ヒント、クラス分割は、アプリの規模と保守性に効果がある範囲で追加する

試作品の段階から過度な抽象化や複雑なアーキテクチャを持ち込まないでください。

## Verification

変更後は、その変更に合った最小限の確認を行ってください。

優先順位は次のとおりです。

1. 構文エラーがない
2. 中心機能が期待どおり動く
3. 既存の主要機能を壊していない
4. エラー時の表示が理解できる
5. READMEの手順で再現できる

実行環境の制約などで確認できない項目がある場合は、未確認であることと、利用者が確認する手順を明示してください。

## Documentation

文書は実装に合わせて更新してください。実装より先に詳細文書を完成させる必要はありません。

継続利用する段階では、READMEに少なくとも次を残します。

- 目的
- 主な機能
- 入力と出力
- セットアップ方法
- 起動方法
- 基本的な使い方
- ディレクトリ構成
- 設定方法
- 確認方法
- 既知の制約

設計上の重要な判断には、「何を選んだか」だけでなく「なぜ選んだか」を短く残してください。

## Completion

次の条件を満たしたら、いったん完成として区切ってください。

- 最初の完成条件を満たしている
- 利用者が実行方法を理解できる
- 確認結果または未確認事項が明確である
- 必須作業と将来の改善候補が分かれている
- READMEなどの記録が現在の実装と一致している

完了時の報告には、次の4点を含めてください。

1. 完成したこと
2. 利用者が確認すること
3. 今回は対応していないこと
4. 次に進めるなら最も効果の高い1項目

改善候補が残っていても、最初の完成条件を満たしていれば「完成」と伝えて構いません。
